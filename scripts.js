var a = 5,
    b = 10,
    value = (a * a) - (2 * a * b) + (b * b);

if (value > 0) {
	console.log('number is over 0')
}
else if (value < 0) {
	console.log('number is below 0')
}
else {
	console.log('number equal 0')
}